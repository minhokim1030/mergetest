let adder = function (x, y) {
    return x + y;
  };
  let divide = function (x, y) {
    return x / y;
  };
  let multiply = function (x, y) {
    return x * y;
  };
  function remainder (x, y) {
    return x % y;
  };
  function addpm (x, y) {
    return (x - x) / y;
  };

  function test (x) {
    return null;
  }
 