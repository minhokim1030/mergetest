let adder = function (x, y) {
    return x + y;
  };
  var divide = function (x, y) {
    return x / y;
  };
  let multipart = function (x, y) {
    return x * y;
  };
  function remainder (x, y) {
    return x % y;
  };
  function addpm (x, y) {
    return (x - x) / y;
  };

  function test (te1) {
    return null;
  }
 
