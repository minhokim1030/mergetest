let adder = function (x) {
    return x + y / c;
  };
  
  let multipart = function (x, y) {
    return x * y;
  };

  var dive = function (x, y) {
    return x / y;
  };
  
  function remainder (x, y) {
    return x % y;
  };
  function addpm (x, y) {
    return (x - x) / y;
  };

  function test (y) {
    return result;
  }
 
